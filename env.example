# Choose LLM backend: mock | snowflake_cortex | openai
LLM_BACKEND=mock

# ---------- Snowflake Cortex (if LLM_BACKEND=snowflake_cortex) ----------
SNOWFLAKE_ACCOUNT=
SNOWFLAKE_USER=
SNOWFLAKE_ROLE=
SNOWFLAKE_WAREHOUSE=
SNOWFLAKE_DATABASE=
SNOWFLAKE_SCHEMA=

# Auth method:
# - keypair: recommended (no password in .env)
# - password: legacy fallback
SNOWFLAKE_AUTH_METHOD=keypair

# --- Key-pair auth (recommended) ---
# Path to your PEM private key (e.g. ~/.ssh/snowflake_rsa_key.p8)
SNOWFLAKE_PRIVATE_KEY_PATH=
# If your key is encrypted, set the passphrase; otherwise leave blank.
SNOWFLAKE_PRIVATE_KEY_PASSPHRASE=

# --- Password auth (fallback) ---
SNOWFLAKE_PASSWORD=

# Example Cortex model names vary by account/region.
# Common examples: llama3.1-70b, mistral-large2, snowflake-arctic
SNOWFLAKE_CORTEX_MODEL=llama3.1-70b

# ---------- OpenAI (if LLM_BACKEND=openai) ----------
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# ---------- Salesforce (optional) ----------
SALESFORCE_LOGIN_URL=https://login.salesforce.com
SALESFORCE_USERNAME=
SALESFORCE_PASSWORD=
SALESFORCE_SECURITY_TOKEN=

# If you have Opportunity IDs, putting them in the UI per transcript is the safest way to avoid name-matching ambiguity.

# Solution Assessment mapping (you will likely need to adjust these for your org)
# The object holding the "Solution Assessment" fields shown in SFDC UI (often a custom object).
SALESFORCE_SOLUTION_ASSESSMENT_OBJECT_API_NAME=
# Lookup field on that object that points back to Opportunity (often Opportunity__c).
SALESFORCE_SOLUTION_ASSESSMENT_OPPORTUNITY_LOOKUP_FIELD_API_NAME=
# Field API name for "Opportunity Comments" on the Solution Assessment object.
SALESFORCE_SOLUTION_ASSESSMENT_OPPORTUNITY_COMMENTS_FIELD_API_NAME=

